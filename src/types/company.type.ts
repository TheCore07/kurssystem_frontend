export type CompanyType = {
    id: string,
    name: string,
    address: string,
    comment: string,
};

export type NewCompanyType = {
    name: string,
    address: string,
    comment: string,
}